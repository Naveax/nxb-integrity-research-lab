name: Validate

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  static-analysis:
    name: Static analysis and smoke validation
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install JSON Schema validator
        shell: pwsh
        run: python -m pip install --disable-pip-version-check jsonschema

      - name: Guard public repository content
        shell: pwsh
        run: ./scripts/Test-PublicRepositoryContent.ps1

      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: |
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted
          Install-Module PSScriptAnalyzer -Scope CurrentUser -Force
          Import-Module PSScriptAnalyzer -Force

      - name: Analyze PowerShell
        shell: pwsh
        run: |
          $results = Invoke-ScriptAnalyzer `
            -Path @('./scripts', './tests') `
            -Recurse `
            -Settings ./.github/PSScriptAnalyzerSettings.psd1
          $results | Format-Table -AutoSize
          if ($results) {
            throw "PSScriptAnalyzer reported $($results.Count) issue(s)."
          }

      - name: Repository smoke validation
        shell: pwsh
        run: ./scripts/Test-Repository.ps1

  lifecycle-powershell-7:
    name: Lifecycle - PowerShell 7
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Pester 5
        shell: pwsh
        run: |
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted
          Install-Module Pester -MinimumVersion 5.5.0 -Scope CurrentUser -Force -SkipPublisherCheck
          Import-Module Pester -MinimumVersion 5.5.0 -Force

      - name: Run Pester
        shell: pwsh
        run: Invoke-Pester -Path ./tests -CI

  lifecycle-windows-powershell-5-1:
    name: Lifecycle - Windows PowerShell 5.1
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Pester 5
        shell: powershell
        run: |
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted
          Install-Module Pester -MinimumVersion 5.5.0 -Scope CurrentUser -Force -SkipPublisherCheck
          Import-Module Pester -MinimumVersion 5.5.0 -Force

      - name: Run Pester
        shell: powershell
        run: Invoke-Pester -Path ./tests -CI
